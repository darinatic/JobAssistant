# VCS + envs
.git
.github
.venv
venv
env

# python
__pycache__
*.py[cod]
.pytest_cache
.mypy_cache
.ruff_cache
build
dist
*.egg-info

# node / frontend (rebuilt inside the image)
frontend/node_modules
frontend/dist

# secrets + local data — NEVER ship these
.env
*.pdf
master_cv.md
outputs
browser_data
artifacts
notebooks
scripts

# docs/tests not needed at runtime
tests
*.md
!README.md

# The fused source model is split (scripts/split_fit_predictor.py) into
# encoder.onnx + head.onnx, which are what's served. Keep model.onnx in git for
# re-splitting, but don't ship the 90MB source in the image.
models/fit-predictor/model.onnx
