# Local dev only (Vite loads this in `npm run dev`, never in `npm run build`).
# In production the SPA is served by FastAPI on the same origin, so the base URL
# is relative (empty) — see frontend/src/lib/api.ts.
VITE_API_URL=http://127.0.0.1:8000
